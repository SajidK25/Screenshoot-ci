name: Dispatch screenshots-action workflow
on:
  workflow_dispatch:
    inputs:
      url:
        description: 'The website URL (ex: https://www.google.com/)'
        required: true
        default: ''

jobs:
  screenshots:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install puppeteer-headful
      uses: mujo-code/puppeteer-headful@master
      env:
        CI: 'true'
    - name: screenshots-ci-action
      uses: flameddd/screenshots-ci-action@master
      with:
        url: ${{ github.event.inputs.url }}
    - name: ImageMagick Action
      uses: jruipinto/ImageMagick-action@v1
      with:
        # ImageMagick command to be executed
        command: mogrify -path -auto-orient -resize 800x450 *.*
    - uses: actions/upload-artifact@v2
      with:
        path: screenshots
        name: Download-screenshots
    # - name: ImageMagick Action
    #   uses: jruipinto/ImageMagick-action@v1
    #   with:
    #     # ImageMagick command to be executed
    #     command: mogrify -path ./screenshots -auto-orient -resize 800x450 ./screenshots/*.*